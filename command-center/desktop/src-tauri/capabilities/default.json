{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window: Tauri core defaults, local OS notifications, and the updater (G7 self-update — check + download + install). No shell, filesystem, arbitrary HTTP, dialog, or clipboard access is granted — the webview reaches the backend only via ordinary fetch/SSE (constrained by the CSP connect-src allow-list), and the updater fetches only its build-time endpoint.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default"
  ]
}
